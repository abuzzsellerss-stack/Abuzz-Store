import { ToolLoadingAnimation } from '@/components/ToolLoadingAnimation';

export default function Loading() {
  return <ToolLoadingAnimation size="fullscreen" message="Loading Industrial Security Gateway..." />;
}
