import { ToolLoadingAnimation } from '../../components/ToolLoadingAnimation';

export default function Loading() {
  return <ToolLoadingAnimation size="fullscreen" message="Authenticating Enterprise Profile..." />;
}
